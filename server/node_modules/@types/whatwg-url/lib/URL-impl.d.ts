import { Globals as GlobalObject } from "webidl-conversions";
import { implementation as URLSearchParamsImpl } from "./URLSearchParams-impl";

export class URLImpl {
  constructor(globalObject: GlobalObject, constructorArgs: [url: string, base?: string]) {
    // Implement the constructor here
  }

  href: string;
  readonly origin: string;
  protocol: string;
  username: string;
  password: string;
  host: string;
  hostname: string;
  port: string;
  pathname: string;
  search: string;
  readonly searchParams: URLSearchParamsImpl;
  hash: string;

  toJSON(): string {
    // Implement the toJSON method here
  }
}

// If you want to use the URLImpl class as an implementation detail, you can export it as such:
export { URLImpl as Implementation };
