'use strict';

const defineProperty = (() => {
  try {
    // Use Object.defineProperty if it's available and working
    return Object.defineProperty;
  } catch (e) {
    // If it throws an error, return a no-op function
    return () => {};
  }
})();

module.exports = defineProperty;
