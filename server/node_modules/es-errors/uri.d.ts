// Define the URIError constructor
class URIError extends Error {
  constructor(message?: string) {
    super(message);
    this.name = "URIError";
  }
}

// Export the URIError constructor
export default URIError;
