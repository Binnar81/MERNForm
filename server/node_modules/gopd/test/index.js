'use strict';

const test = require('tape');
const gOPD = require('../');

test('gOPD', function (t) {
	if (typeof gOPD !== 'function') {
		t.test('gOPD is not supported', function (st) {
			st.notOk(gOPD, 'gOPD is not a function');
			st.end();
		});
		return;
	}

	t.test('gOPD is supported', function (st) {
		st.equal(typeof gOPD, 'function', 'gOPD is a function');

		const obj = { x: 1 };
		st.ok('x' in obj, 'property exists');

	
